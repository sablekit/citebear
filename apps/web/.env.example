# Python API origin (server-side only; the browser never calls it directly)
API_URL=http://localhost:8000

# Shared secret for the web -> api hop (must match the api's INTERNAL_API_KEY)
INTERNAL_API_KEY=change-me
